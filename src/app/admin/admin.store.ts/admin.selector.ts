// // selector.ts
// import { createFeatureSelector, createSelector } from "@ngrx/store";
// import { HomeState } from "./home.state";


// export const selectHomeState = createFeatureSelector<HomeState>('home');

// export const selectPageState = createSelector(selectHomeState, (state: HomeState) => state.pageState);
// export const selectLoading = createSelector(selectHomeState, (state: HomeState) => state.loading);

// export const selectError = createSelector(selectHomeState, (state: HomeState) => state.error);
// export const selectInfo = createSelector(selectHomeState, (state: HomeState) => state.info);